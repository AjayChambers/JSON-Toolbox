# JSON TOOLBOX TODO LIST

1. Add Linux config & build tasks to ".vscode/tasks.json"

2. Add run tasks for JSON-Toolbox's JBox (jbox) cli executable runtime binary.

3. Add comment header to "cmake/module/test-mod/gtest-for-clang-cl.cmake" and add doc comment for the
`gtest_for_clang_cl()` function.

4. Configure CMakePresets.json 'Build Preset Configurations' such that unit test are not included as part
of the targets being built.
